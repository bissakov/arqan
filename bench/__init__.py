"""Benchmarks and stress runs for arqan.

The test suite answers whether the program is correct; this package answers
what it costs and whether it survives abuse. Both drive the same pty harness
in `tests/`, so a benchmark is an ordinary session with a probe on it.
"""
