# Commits that only reformat, ignored by:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add the hash of the commit as it lands on main, not the local one: the
# squash merge rewrites it.
#
# (style): format the tree with clang-format
